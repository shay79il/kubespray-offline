--- a/roles/kubespray_defaults/vars/el8_igz_dnf.yml	2026-07-02 23:31:57
+++ b/roles/kubespray_defaults/vars/el8_igz_dnf.yml	2026-07-02 23:31:55
@@ -0,0 +1,2 @@
+---
+el8_igz_dnf: "{{ ansible_os_family == 'RedHat' and ansible_distribution_major_version | int == 8 }}"
--- a/roles/system_packages/tasks/main.yml	2026-04-24 07:16:46
+++ b/roles/system_packages/tasks/main.yml	2026-07-02 23:31:55
@@ -23,19 +23,29 @@
     - ansible_os_family == "RedHat"
     - not is_fedora_coreos
 
+- name: Install epel-release on RHEL derivatives (EL8 igz_dnf)
+  igz_dnf:
+    name: epel-release
+    state: present
+  when:
+    - el8_igz_dnf | default(false) | bool
+    - not is_fedora_coreos
+    - epel_enabled | bool
+  tags:
+    - bootstrap_os
+
 - name: Install epel-release on RHEL derivatives
   package:
     name: epel-release
     state: present
   when:
     - ansible_os_family == "RedHat"
+    - not (el8_igz_dnf | default(false) | bool)
     - not is_fedora_coreos
     - epel_enabled | bool
   tags:
     - bootstrap_os
 
-# Remove this after ansible-core >= 2.19.0
-# See https://github.com/kubernetes-sigs/kubespray/pull/12138#issuecomment-3019304574
 - name: Install python3-libdnf5 on Fedora >= 41
   raw: >
     dnf install --assumeyes python3-libdnf5
@@ -45,17 +55,45 @@
     - ansible_distribution == "Fedora"
     - ansible_distribution_major_version | int >= 41
 
+- name: Manage packages (EL8 igz_dnf)
+  igz_dnf:
+    name: "{{ item.packages }}"
+    state: "{{ item.state }}"
+    update_cache: "{{ true if ansible_pkg_mgr in ['zypper', 'apt', 'dnf'] else false }}"
+  register: pkgs_task_result
+  until: pkgs_task_result is succeeded
+  retries: "{{ pkg_install_retries }}"
+  delay: "{{ retry_stagger | random + 3 }}"
+  when:
+    - el8_igz_dnf | default(false) | bool
+    - ansible_os_family not in ["Flatcar", "Flatcar Container Linux by Kinvolk"]
+    - not is_fedora_coreos
+    - item.packages != []
+  loop:
+    - packages: "{{ pkgs_to_remove | dict2items | selectattr('value', 'ansible.builtin.all') | map(attribute='key') }}"
+      state: "absent"
+      action_label: "remove"
+    - packages: "{{ pkgs | dict2items | selectattr('value', 'ansible.builtin.all') | map(attribute='key') }}"
+      state: "present"
+      action_label: "install"
+  loop_control:
+    label: "{{ item.action_label }}"
+  tags:
+    - bootstrap_os
+  timeout: "{{ pkg_install_timeout }}"
+
 - name: Manage packages
   package:
     name: "{{ item.packages }}"
     state: "{{ item.state }}"
     update_cache: "{{ true if ansible_pkg_mgr in ['zypper', 'apt', 'dnf'] else omit }}"
-    cache_valid_time: "{{ 86400 if ansible_pkg_mgr == 'apt' else omit }}" # 24h
+    cache_valid_time: "{{ 86400 if ansible_pkg_mgr == 'apt' else omit }}"
   register: pkgs_task_result
   until: pkgs_task_result is succeeded
   retries: "{{ pkg_install_retries }}"
   delay: "{{ retry_stagger | random + 3 }}"
   when:
+    - not (el8_igz_dnf | default(false) | bool)
     - ansible_os_family not in ["Flatcar", "Flatcar Container Linux by Kinvolk"]
     - not is_fedora_coreos
     - item.packages != []
--- a/roles/kubernetes/preinstall/tasks/0081-ntp-configurations.yml	2026-04-24 07:16:46
+++ b/roles/kubernetes/preinstall/tasks/0081-ntp-configurations.yml	2026-07-02 23:31:57
@@ -64,12 +64,23 @@
     state: started
     enabled: true
 
+- name: Ensure tzdata package (EL8 igz_dnf)
+  igz_dnf:
+    name: tzdata
+    state: present
+  when:
+    - el8_igz_dnf | default(false) | bool
+    - ntp_timezone
+    - not is_fedora_coreos
+    - not ansible_os_family in ["Flatcar", "Flatcar Container Linux by Kinvolk"]
+
 - name: Ensure tzdata package
   package:
     name:
       - tzdata
     state: present
   when:
+    - not (el8_igz_dnf | default(false) | bool)
     - ntp_timezone
     - not is_fedora_coreos
     - not ansible_os_family in ["Flatcar", "Flatcar Container Linux by Kinvolk"]
--- a/roles/kubernetes-apps/helm/tasks/main.yml	2026-04-24 07:16:46
+++ b/roles/kubernetes-apps/helm/tasks/main.yml	2026-07-02 23:31:57
@@ -13,11 +13,21 @@
         - ../vars
       skip: true
 
+- name: Helm | Install PyYaml (EL8 igz_dnf)
+  igz_dnf:
+    name: "{{ pyyaml_package }}"
+    state: present
+  when:
+    - el8_igz_dnf | default(false) | bool
+    - pyyaml_package is defined
+
 - name: Helm | Install PyYaml
   package:
     name: "{{ pyyaml_package }}"
     state: present
-  when: pyyaml_package is defined
+  when:
+    - not (el8_igz_dnf | default(false) | bool)
+    - pyyaml_package is defined
 
 - name: Helm | Install PyYaml [flatcar]
   include_tasks: pyyaml-flatcar.yml
--- a/roles/network_plugin/calico/tasks/install.yml	2026-04-24 07:16:46
+++ b/roles/network_plugin/calico/tasks/install.yml	2026-07-02 23:31:57
@@ -1,4 +1,16 @@
 ---
+- name: Calico | Install Wireguard packages (EL8 igz_dnf)
+  igz_dnf:
+    name: "{{ item }}"
+    state: present
+  with_items: "{{ calico_wireguard_packages }}"
+  register: calico_package_install
+  until: calico_package_install is succeeded
+  retries: 4
+  when:
+    - calico_wireguard_enabled
+    - el8_igz_dnf | default(false) | bool
+
 - name: Calico | Install Wireguard packages
   package:
     name: "{{ item }}"
@@ -7,7 +19,9 @@
   register: calico_package_install
   until: calico_package_install is succeeded
   retries: 4
-  when: calico_wireguard_enabled
+  when:
+    - calico_wireguard_enabled
+    - not (el8_igz_dnf | default(false) | bool)
 
 - name: Calico | Copy calicoctl binary from download dir
   copy:
